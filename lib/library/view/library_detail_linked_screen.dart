import 'package:flutter/material.dart';

class LibraryDetailLinkedScreen extends StatelessWidget {
  const LibraryDetailLinkedScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('연계독서'),
    );
  }
}
