import 'package:flutter/material.dart';

class LibraryDetailActivityLogScreen extends StatelessWidget {
  const LibraryDetailActivityLogScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('활동일지'),
    );
  }
}
